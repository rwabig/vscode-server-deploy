###############################################################################
# AUTHELIA USERS DATABASE (FILE BACKEND)
# Passwords MUST be Argon2id hashes — NEVER plaintext
###############################################################################

users:
{% for username, password in authelia_users.items() %}
  {{ username }}:
    displayname: "{{ username }}"
    password: "{{ password | password_hash('argon2id') }}"
    email: "{{ username }}@{{ domain_name }}"
    groups:
      - users
{% endfor %}
